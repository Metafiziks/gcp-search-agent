# Observability dependencies
# Install with: pip install -r observability/requirements.txt

# Hallucination detection (HHEM-2.1)
transformers>=4.40.0
torch>=2.0.0          # CPU-only is fine for eval runner

# Anomaly detection
scikit-learn>=1.3.0
numpy>=1.24.0

# BigQuery telemetry sink
google-cloud-bigquery>=3.0.0

# GCS model persistence
google-cloud-storage>=2.0.0
